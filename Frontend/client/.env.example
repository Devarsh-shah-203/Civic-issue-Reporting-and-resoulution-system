# Point these at your backend once it's built.
# Until then, the app runs entirely on a mock, localStorage-backed
# data layer (see services/mockDb.js) — no backend required to demo.

NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api
NEXT_PUBLIC_SOCKET_URL=http://localhost:5000
